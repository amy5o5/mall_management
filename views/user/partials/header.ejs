<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
  <meta charset="UTF-8">
  <title>پاساژ</title>
  <link rel="stylesheet" href="/CSS/management.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</head>
<body>
  <!-- هدر -->
  <div class="header-container">
    <!-- بنر اسلایدر -->
    <div id="bannerCarousel" class="carousel slide banner-carousel mb-4" data-bs-ride="carousel">
      <div class="carousel-inner">
        <div class="carousel-item active">
          <img src="/banner/images.jpeg" alt="بنر 1">
        </div>
        <div class="carousel-item">
          <img src="/banner/images2.jpeg" alt="بنر 2">
        </div>
      </div>
      <button class="carousel-control-prev" type="button" data-bs-target="#bannerCarousel" data-bs-slide="prev">
        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
        <span class="visually-hidden">قبلی</span>
      </button>
      <button class="carousel-control-next" type="button" data-bs-target="#bannerCarousel" data-bs-slide="next">
        <span class="carousel-control-next-icon" aria-hidden="true"></span>
        <span class="visually-hidden">بعدی</span>
      </button>
    </div>
    
    <br><br>
    
    <!-- تبلیغات -->
    <div class="ad-box mb-4">
      <h3>تبلیغات</h3>
      <p>اینجا می‌توانید تبلیغات خود را قرار دهید.</p>
    </div>

    <div class="nav-buttons d-flex justify-content-around mb-4">
      <button class="btn btn-primary">واحدهای تجاری</button>
      <button class="btn btn-success">نقشه پاساژ</button>
      <button class="btn btn-secondary">غرفه‌های پاساژ</button>
    </div>

    <!-- دکمه‌های ورود/خروج -->
    <div class="nav-buttons d-flex justify-content-around mb-4">
      <% if (!iSuser && !seller && currentUrl !== "/shk/edit-shop" && currentUrl !== "/user/profile") { %>
        <form action="/logins" method="GET">
          <button type="submit" class="btn-custom">ورود/ثبت‌نام</button>
        </form>
      <% } %>

      <% if (iSuser && currentUrl == "/user/profile") { %>
        <form action="/user/userAuth/logout" method="GET">
          <button type="submit" class="btn-custom">خروج</button>
        </form>
      <% } %>

      <% if (iSuser && currentUrl !== "/user/profile" && currentUrl !== "/shk/edit-shop") { %>
        <form action="/user/profile/" method="GET">
          <button type="submit" class="btn-custom">پروفایل</button>
        </form>
        <form action="/user/userAuth/logout" method="GET">
          <button type="submit" class="btn-custom">خروج</button>
        </form>
      <% } %>

      <% if (seller && currentUrl == "/shk/edit-shop") { %>
        <form action="/shk/shopkeeperAuth/logout" method="GET">
          <button type="submit" class="btn-custom">خروج</button>
        </form>
      <% } %>

      <% if (seller && currentUrl !== "/shk/edit-shop") { %>
        <form action="/shk/edit-shop" method="GET">
          <button type="submit" class="btn-custom">پروفایل</button>
        </form>
        <form action="/shk/shopkeeperAuth/logout" method="GET">
          <button type="submit" class="btn-custom">خروج</button>
        </form>
      <% } %>
    </div>
  </div>
  <script>
      document.querySelectorAll('.category-btn').forEach(btn => {
    btn.addEventListener('click', function () {
      document.querySelectorAll('.category-btn').forEach(b => b.classList.remove('selected'));
      this.classList.add('selected');
    });
  });
  </script>
</body>
</html>
